import { MakeCircularDirective } from './make-circular.directive';

describe('MakeCircularDirective', () => {
  it('should create an instance', () => {
    const directive = new MakeCircularDirective();
    expect(directive).toBeTruthy();
  });
});
