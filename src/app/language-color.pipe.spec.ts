import { LanguageColorPipe } from './language-color.pipe';

describe('LanguageColorPipe', () => {
  it('create an instance', () => {
    const pipe = new LanguageColorPipe();
    expect(pipe).toBeTruthy();
  });
});
