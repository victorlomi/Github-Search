import { CommaSeparatePipe } from './comma-separate.pipe';

describe('CommaSeparatePipe', () => {
  it('create an instance', () => {
    const pipe = new CommaSeparatePipe();
    expect(pipe).toBeTruthy();
  });
});
